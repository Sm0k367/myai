document.addEventListener('DOMContentLoaded', () => {
    console.log('Welcome to MyAI Assistant!');
    
    // Initialize modules
    initTaskManager();
    initScheduler();
    initInfoRetrieval();
    initVoiceInteraction();
    initAuth();
});
